apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: affine-migration
  name: affine-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: affine-migration
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c
        kompose.version: 1.38.0 (HEAD)
      labels:
        io.kompose.service: affine-migration
    spec:
      containers:
        - args:
            - sh
            - -c
            - node ./scripts/self-host-predeploy.js
          env:
            - name: AFFINE_INDEXER_ENABLED
              value: "false"
            - name: DATABASE_URL
              value: postgresql://affine:random-database-password@postgres:5432/affine
            - name: REDIS_SERVER_HOST
              value: redis
          envFrom:
            - configMapRef:
                name: env
          image: ghcr.io/toeverything/affine:stable
          name: affine-migration-job
          volumeMounts:
            - mountPath: /root/.affine/storage
              name: affine-migration-claim0
            - mountPath: /root/.affine/config
              name: affine-migration-claim1
      restartPolicy: Always
      volumes:
        - name: affine-migration-claim0
          persistentVolumeClaim:
            claimName: affine-migration-claim0
        - name: affine-migration-claim1
          persistentVolumeClaim:
            claimName: affine-migration-claim1

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-claim1
  namespace: affine
  labels:
    app: affine
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-hdd
  resources:
    requests:
      storage: 1Gi
